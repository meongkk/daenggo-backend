*** Begin Patch
*** Update File: .env
@@
-ALLOW_UNAUTHENTICATED_DEVELOPMENT_API=true
-
*** Update File: compose.yaml
@@
-      ALLOW_UNAUTHENTICATED_DEVELOPMENT_API: "${ALLOW_UNAUTHENTICATED_DEVELOPMENT_API:-false}"
*** Update File: src/main/java/com/daenggo/backend/global/config/SecurityConfig.java
@@
-import org.springframework.beans.factory.annotation.Value;
@@
-    public SecurityFilterChain securityFilterChain(
-            final HttpSecurity http,
-            @Value("${ALLOW_UNAUTHENTICATED_DEVELOPMENT_API:false}")
-            final boolean allowUnauthenticatedDevelopmentApi
-    ) throws Exception {
+    public SecurityFilterChain securityFilterChain(final HttpSecurity http) throws Exception {
@@
-                .authorizeHttpRequests(authorize -> {
-                    authorize
+                .authorizeHttpRequests(authorize -> authorize
@@
-                            ).permitAll();
-
-                    // 로그인 프론트가 완성되기 전 로컬 Docker에서만 임시 사용자 API를 허용한다.
-                    if (allowUnauthenticatedDevelopmentApi) {
-                        authorize.requestMatchers(
-                                "/api/community/**",
-                                "/api/walks/**"
-                        ).permitAll();
-                    } else {
-                        authorize.requestMatchers(
-                                "/api/community/**",
-                                "/api/walks/**"
-                        ).authenticated();
-                    }
-
-                    authorize
-                            .requestMatchers(
+                            ).permitAll()
+                            .requestMatchers(
                                     "/api/users/**",
                                     "/api/pets/**",
                                     "/api/groups/**",
                                     "/api/group-invitations/**",
+                                    "/api/community/**",
+                                    "/api/walks/**",
                                     "/api/favorites/**"
                             ).authenticated()
-                            .anyRequest().permitAll();
-                })
+                            .anyRequest().permitAll())
*** End Patch
